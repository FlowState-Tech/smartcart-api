package com.smartcart.notification.domain.model.valueobjects;

/**
 * Channel used for outbound notifications (ubiquitous language from Notification context).
 */
public enum ChannelType {
    PUSH,
    EMAIL,
    SMS
}
